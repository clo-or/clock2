제공해주신 자료를 바탕으로 파이썬의 sktime 및 statsmodels 등을 활용한 시계열 분석 프로세스(데이터 준비 및 정상성 확인, 전처리, 모형 구축 및 평가)를 체계적으로 요약해 드립니다.1. 시계열 데이터의 이해와 정상성 검정1) 시계열 데이터의 특징정의: 시간에 따라 순차적으로(sequentially) 관측한 시퀀스 데이터입니다.  시퀀스와의 차이: 일반 시퀀스는 순서만 중요하지만, 시계열 데이터는 데이터의 순서뿐만 아니라 관측(발생) 시간이 중요한 의미를 가지며 시간적 절대 영점이 존재합니다.  정상성 시계열 (Stationary): 평균과 분산 등 확률적 특성이 시간에 따라 변하지 않는 시계열입니다. 예측은 "과거 패턴이 미래에도 반복된다"는 가정에 기반하므로 정상성 확보가 중요합니다.  2) 통계적 검정 방법ADF 검정 (Augmented Dickey-Fuller test): 단위근(비정상성 특성)의 존재 여부를 검정합니다.  $p\text{-value} < 0.05$이면 귀무가설을 기각하여 정상성 시계열로 판단합니다.  Ljung-Box 검정: 데이터의 자기상관성 및 백색잡음(White Noise) 여부를 검정합니다.  $p\text{-value} < 0.05$이면 자기상관성이 존재하므로 모형을 통한 개선이 가능함을 뜻하며, $p\text{-value} \ge 0.05$이면 더 이상 패턴이 없는 백색잡음 상태로 추가 모형이 불필요합니다.  2. 시계열 데이터 전처리 (Preprocessing)시계열 데이터 전처리는 크게 결측치 처리, 이상치 처리, 디노이징의 3단계로 진행됩니다. 시계열은 연속성과 통계적 특성(평균·분산의 변화)을 유지해야 하므로 단순 제거보다는 주변 데이터를 활용한 대체(Imputation) 방식을 사용해야 합니다.  1) 결측치 처리 (Missing Values)LOCF / NOCB: 직전 관측값으로 대체(ffill)하거나 직후 관측값으로 대체(bfill)합니다.  Moving Average: 직전 $m$개 데이터의 이동평균으로 대체합니다.  보간법 (Interpolation): 선형 및 비선형 보간을 적용합니다.  sktime 활용: Imputer(method="linear") 또는 method="forecaster"를 통해 지수평활법 기반 대치 등이 가능합니다.  2) 이상치 처리 (Outliers)Hampel Filter: Sliding Window 구간 내의 Median Absolute Deviation(MdAD)을 구하여 중앙값 대비 변동이 큰 이상치를 탐색하고 중앙값으로 대체합니다.  잔차 기반 탐색 (STL + G-ESD): STLForecaster 등으로 계절성과 추세를 제거한 잔차(Residuals)를 추출한 후, Generalized Extreme Studentized Deviate(G-ESD) 통계 검정을 적용하여 이상치를 찾아내고 선형보간 등으로 보정합니다.  3) 잡음 제거 (Denoising)평활화: 단순이동평균(SMA)이나 최근 데이터에 가중치를 두는 지수이동평균(EMA)을 사용하여 단기 변동을 완화합니다.  주파수 필터링: 고주파(High frequency) 성분인 노이즈를 제거하기 위해 고속 푸리에 변환(FFT) 후 Filter(h_freq=20) 등을 적용하여 저주파 통과 필터(LPF)를 구현합니다.  3. 시계열 예측 모형 (Forecasting Models)1) 평활법 (Smoothing)단순지수평활 (Simple ES): 추세와 계절성이 없는 데이터의 단기 예측에 활용하며, 평활계수 $\alpha$가 1에 가까울수록 최근 데이터를 강하게 반영합니다.  Holt's Model: 지수평활에 선형 추세(Trend) 반영을 추가한 이중 지수평활법입니다.  Holt-Winter's Model: 추세와 계절성(Seasonal)을 모두 반영하는 모형으로, 변동 폭에 따라 가법적(Additive) 또는 승법적(Multiplicative) 모형을 선택합니다.  2) ARIMA / SARIMA 모형비정상성 시계열을 차분($d$)하여 정상화한 후, 과거 관측값 기반의 $AR(p)$ 성분과 과거 오값 기반의 $MA(q)$ 성분을 결합하여 예측하는 대표적인 통계 모형입니다.  $$y_{t}^{\prime} = c + \phi_{1}y_{t-1}^{\prime} + \dots + \phi_{p}y_{t-p}^{\prime} + \theta_{1}\epsilon_{t-1} + \dots + \theta_{q}\epsilon_{t-q} + \epsilon_{t}$$차수 결정 ($p, d, q$): 자기상관함수(ACF)와 편자기상관함수(PACF)의 그래프가 급격히 감소(절단)하는 시점을 기준으로 결정합니다.  SARIMA: 주기적 패턴이 존재할 때 계절성 시차 성분($(P, D, Q)_{sp}$)을 추가하여 확장한 모형입니다.  AutoARIMA: ACF/PACF 해석의 모호함을 해결하기 위해, 여러 차수를 자동 탐색하며 정보 규준(AIC/BIC)이 가장 작은 최적의 모형을 찾아줍니다.  4. 모형 검증 및 평가 프로세스sktime 라이브러리는 scikit-learn과 유사한 API 구조(fit, predict)를 차용하여 체계적인 시계열 머신러닝 프로세스를 지원합니다.  1) 기본 예측 흐름데이터 분할: 시간 순서(Temporal)를 유지하며 temporal_train_test_split(ts, test_size=36)으로 학습 및 평가 데이터를 나눕니다.  시평 설정: 미래를 예측할 상대적/절대적 시점의 길이인 ForecastingHorizon을 설정합니다.  모형 학습 및 예측: forecaster.fit(y_train) 후 predict(fh)를 수행합니다.  성능 평가: 오차 평가지표를 계산하여 베이스라인(단순 예측 등) 및 타 모형과 성능을 비교합니다.  2) 성능 평가 지표 (Error Metrics)지표풀네임특징 및 스케일 의존 여부주요 용도MAEMean Absolute Error스케일 의존 / 이상치 보통  직관적인 오차 크기 평가  RMSERoot Mean Squared Error스케일 의존 / 이상치 민감  큰 오차에 패널티를 부여할 때  概念Mean Absolute Percentage Error스케일 독립 (%) / $y_t \approx 0$ 시 발산  서로 다른 척도(단위) 간의 비교  SMAPESymmetric MAPE스케일 독립 (%) / 비대칭성 보완  MAPE의 단점 보완 및 척도 비교  MASEMean Absolute Scaled Error스케일 독립 (비율) / 0 근방 안정  실제값이 0에 가까운 경우 안정적 평가  TSTracking Signal편향성 진단 지표 ($TS평가 구간 운영: 패턴이 자주 변하는 시계열은 고정 크기 윈도우를 이동시키는 Rolling Horizon 방식을, 장기적으로 안정된 시계열은 데이터를 누적해가는 Expanding 방식을 채택하여 평가합니다.  

다른 LLM 및 AI 시스템이 컨텍스트 파싱, API 매핑, 아키텍처 설계를 즉시 수행할 수 있도록 Python 핵심 API와 Darts 라이브러리의 구현 코드 위주로 요약한 가이드 문서입니다.

---

### 1. Data Structure & Type Conversion (TimeSeries)

Darts는 고유 데이터 유형인 `TimeSeries` 구조를 기반으로 연산합니다. 

```python
from darts import TimeSeries
from darts.datasets import AirPassengersDataset

# 1. Load Dataset to TimeSeries Object
[cite_start]ts = AirPassengersDataset().load() # shape: (time, component, sample) [cite: 131, 147]

# 2. TimeSeries Properties
[cite_start]indices = ts.time_index     # pandas.DatetimeIndex or pandas.RangeIndex [cite: 102, 327]
[cite_start]frequency = ts.freq         # Slicing frequency (e.g., <MonthBegin>) [cite: 328, 350]
[cite_start]steps = ts.n_timesteps      # Total time steps [cite: 329]
[cite_start]components = ts.components  # Column Index [cite: 331]

# 3. Type Conversion: To Pandas / Numpy
[cite_start]df = ts.to_dataframe()      # To pandas DataFrame [cite: 222]
[cite_start]arr = ts.values()           # To numpy ndarray [cite: 223]
[cite_start]series = ts.to_series()     # To pandas Series (Univariate only) [cite: 221]

# 4. Type Conversion: From Pandas / Numpy
[cite_start]ts_from_df = TimeSeries.from_dataframe(df) [cite: 277]
[cite_start]ts_from_arr = TimeSeries.from_values(arr) [cite: 276]
[cite_start]ts_from_sr = TimeSeries.from_series(series) [cite: 276]

# 5. Data Splitting & Slicing
[cite_start]ts_train, ts_test = ts[:-36], ts[-36:]                          # Index Slicing [cite: 465]
[cite_start]ts_tr, ts_te = ts.split_before(0.5)                             # Ratio Slicing [cite: 387]
[cite_start]ts_tr, ts_te = ts.split_before(pd.Timestamp("1958-01-01"))      # Datetime Slicing [cite: 444]

```

---

### 2. Preprocessing & Transformation Pipelines

Darts의 모든 변환기(Transformers)는 Scikit-learn 스타일의 `fit_transform()` 및 `inverse_transform()` 명세를 공유합니다. 

```python
from darts.dataprocessing.transformers import Scaler, BoxCox, Diff, Mapper, MissingValuesFiller
from darts.dataprocessing.pipeline import Pipeline
from sklearn.preprocessing import MinMaxScaler
import numpy as np

# 1. Missing Value Imputation
[cite_start]ts_filled = MissingValuesFiller().transform(ts, method="linear") # options: 'linear', 'quadratic', 'mean' [cite: 34, 43]

# 2. Mathematical Transformation
[cite_start]ts_log = ts.map(np.log) # Vectorized mapping [cite: 31]

# 3. Build Transformation Pipeline
[cite_start]mapper_log = Mapper(lambda x: np.log(x)) [cite: 46]
[cite_start]boxcox_05 = BoxCox(lmbda=0.5) [cite: 46]
[cite_start]scaler_minmax = Scaler(MinMaxScaler(feature_range=(-1, 1))) [cite: 46]

[cite_start]pipeline = Pipeline([mapper_log, boxcox_05, scaler_minmax]) [cite: 46]
[cite_start]ts_transformed = pipeline.fit_transform(ts) [cite: 46]
ts_inverted = pipeline.inverse_transform(ts_transformed)

```

---

### 3. Local Forecasting Models (LFM)

단일 대상 시계열 통계 및 수학적 선형 결합을 적용하는 알고리즘군입니다. 

```python
from darts.models import NaiveMovingAverage, ExponentialSmoothing, AutoARIMA
from darts.utils.utils import ModelMode, SeasonalityMode

# 1. Exponential Smoothing Initialization
model_es = ExponentialSmoothing(
    trend=ModelMode.ADDITIVE,
    seasonal=SeasonalityMode.MULTIPLICATIVE,
    seasonal_periods=12
[cite_start]) [cite: 1087, 1089, 1092, 1094]

# 2. AutoARIMA Initialization
[cite_start]model_arima = AutoARIMA(season_length=12) [cite: 1099, 1101]

# 3. Execution Pipeline (Fit -> Predict)
[cite_start]model_es.fit(ts_train) [cite: 1107]
[cite_start]pred_es = model_es.predict(n=36) # Recursive multi-step prediction [cite: 1115]

# 4. Probabilistic Forecasting configuration (LFM)
# Generates Monte Carlo samples inside the predict interface
[cite_start]pred_prob = model_es.predict(n=36, num_samples=100) [cite: 980, 982]

```

---

### 4. Global Forecasting Models & Covariates (GFM)

트리 머신러닝 구조 및 다변량 시계열 결합(Meta-learning) 모델 아키텍처 세팅입니다. 

#### Covariates Multi-Dimensional Stacking

```python
from darts.utils.timeseries_generation import datetime_attribute_timeseries as dt_attr

# Generate Time-dependent Features (Past/Future Covariates)
[cite_start]cov_month = dt_attr(ts, "month", dtype=np.float32) [cite: 976, 977]
[cite_start]cov_year = dt_attr(ts, "year", dtype=np.float32) [cite: 976, 978]
[cite_start]cov_stacked = cov_month.stack(cov_year) # Multi-variate stacking [cite: 31, 3856]
[cite_start]cov_scaled = Scaler().fit_transform(cov_stacked) [cite: 977]

```

#### Linear Regression & Tree Models (Extrapolation Solution)

트리 구조 기반 알고리즘은 Extrapolation(외삽) 취약성이 존재하므로, `diff(1)`(차분) 및 `cumsum()` 연산을 바인딩하여 외삽 실패를 방지합니다. 

```python
from darts.models import LinearRegressionModel, RandomForestModel, XGBModel, LightGBMModel, CatBoostModel

# [cite_start]multi_models=True (Default): Direct strategy (Trains 'h' independent models per horizon) [cite: 2147, 2319]
# [cite_start]multi_models=False: Recursive strategy (Trains 1-step model, recursive feedback loops) [cite: 2147]

model_xgb = XGBModel(
    [cite_start]lags=12,                            # Lookback window for target series [cite: 2175, 2365]
    [cite_start]lags_future_covariates=(12, 12),    # Tuple format: (past lags, future lags) [cite: 974, 2175]
    [cite_start]output_chunk_length=12,             # Intrinsic output sequence length [cite: 968, 2175]
    n_estimators=100,
    learning_rate=0.1
[cite_start]) [cite: 2174, 2175]

# Fit using differenced series to eliminate linear trends
[cite_start]model_xgb.fit(ts_train.diff(1), future_covariates=cov_scaled) [cite: 2175]

# Predict and reconstruct the baseline scale via cumulative summation
[cite_start]pred_xgb_diff = model_xgb.predict(n=36, future_covariates=cov_scaled) [cite: 2175]
[cite_start]pred_xgb = pred_xgb_diff.cumsum() + ts_train[-1] # Inverse differencing mapping [cite: 2169, 2170]

# GFM Probabilistic Settings via Likelihood mapping
model_lr_prob = LinearRegressionModel(
    lags=12,
    likelihood="quantile",
    quantiles=[0.05, 0.5, 0.95]
[cite_start]) [cite: 984]
[cite_start]model_lr_prob.fit(ts_train) [cite: 984]
[cite_start]pred_lr_prob = model_lr_prob.predict(n=36, num_samples=100) [cite: 984]

```

---

### 5. Deep Learning Models (PyTorch-Lightning Backend)

#### RNN / LSTM / GRU / BlockRNN

* `RNNModel`: Fully RNN framework. `output_chunk_length=1`. Past & Future Covariates supported. 


* `BlockRNNModel`: RNN Encoder + Linear Decoder. Custom `output_chunk_length` configuration. Past Covariates only. 



```python
from darts.models import RNNModel, BlockRNNModel

model_lstm = RNNModel(
    [cite_start]model="LSTM",                       # Options: 'RNN', 'LSTM', 'GRU' [cite: 3895]
    [cite_start]input_chunk_length=12,              # Lookback length [cite: 3894, 3932]
    [cite_start]training_length=24,                 # Sub-sequence length during backprop windows (> input_chunk_length) [cite: 3897, 3940]
    hidden_dim=20,
    dropout=0.1,
    batch_size=16,
    n_epochs=300,
    optimizer_kwargs={"lr": 1e-3},
    model_name="DL_LSTM_REG",
    force_reset=True,
    save_checkpoints=True
[cite_start]) [cite: 3927, 3932, 3937, 3940, 3943, 3946, 3949, 3952, 3955, 3965, 3966, 3967]

[cite_start]model_lstm.fit(ts_train_scaled, future_covariates=cov_scaled) [cite: 3405]

```

#### Transformer / N-BEATS / N-HITS

```python
from darts.models import TransformerModel, NBEATSModel, NHiTSModel

# 1. Self-Attention Transformer
model_tf = TransformerModel(
    input_chunk_length=12,
    output_chunk_length=1,
    [cite_start]d_model=64,                         # Embedding dimension (nhead divisible) [cite: 3438, 3445]
    [cite_start]nhead=8,                            # Multi-head structures [cite: 3438, 3445]
    num_encoder_layers=3,
    num_decoder_layers=3,
    dim_feedforward=128,
    n_epochs=300,
    batch_size=16
[cite_start]) [cite: 3445, 3446]

# 2. Hierarchical Interpolation / Basis Expansion Networks
model_nhits = NHiTSModel(
    input_chunk_length=30,
    output_chunk_length=7,
    [cite_start]num_stacks=10,                      # Frequency sub-sampling allocations [cite: 3462, 3469, 3471, 3473]
    [cite_start]num_blocks=3, [cite: 3471, 3473]
    [cite_start]num_layers=4, [cite: 3471, 3473]
    [cite_start]layer_widths=512, [cite: 3471, 3473]
    [cite_start]n_epochs=100, [cite: 3471, 3473]
    batch_size=800
[cite_start]) [cite: 3473]

[cite_start]model_nhits.fit(train_scaled) [cite: 3473]

```

---

### 6. Evaluation, Backtesting & Ensembles

```python
from darts.metrics import mae, mse, mape, r2_score
from darts.models import NaiveEnsembleModel

# 1. Model Evaluation Metrics
[cite_start]mape_val = mape(ts_test, pred_xgb) [cite: 976, 1158]
[cite_start]r2_val = r2_score(ts_test, pred_xgb) [cite: 3450, 3467]

# 2. Historical Forecasts / Backtesting Execution Interface
# Simulation interface using sliding windows over execution paths
backtest_series = model_es.historical_forecasts(
    series=ts,
    [cite_start]start=0.5,                          # Execution initiates at 50% marker of series [cite: 1317]
    [cite_start]forecast_horizon=12,                # Step sizing per forecast path [cite: 1317]
    [cite_start]stride=1,                           # Evaluation window shift increment [cite: 1299, 1443]
    [cite_start]retrain=False,                      # Freezes parameter configurations [cite: 1302, 1308]
    [cite_start]last_points_only=True               # Emits terminal coordinate transitions [cite: 1310, 1317]
[cite_start]) [cite: 1317]

# 3. Raw Errors Calculation Interface (Statistical Distribution Assessment)
raw_errors = model_es.backtest(
    series=ts,
    start=0.5,
    forecast_horizon=12,
    metric=mape,
    [cite_start]reduction=None,                     # Emits array of scores without spatial mean reduction [cite: 1407, 1445]
    [cite_start]last_points_only=False              # Overlapping step generation [cite: 1447]
[cite_start]) [cite: 1437]

# 4. Naive Ensemble Construction
ensemble = NaiveEnsembleModel(
    forecasting_models=[model_es, model_arima],
    show_warnings=False
[cite_start]) [cite: 2186]
[cite_start]ensemble_backtest = ensemble.historical_forecasts(ts, start=0.6, forecast_horizon=3) [cite: 2186, 2187]

```